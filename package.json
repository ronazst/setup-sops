{
  "name": "setup-sops",
  "description": "Setup CNCF SOPS",
  "version": "1.0.0",
  "author": "Ronazst",
  "license": "MIT",
  "scripts": {
    "build": "tsc -noEmit src/run.ts && esbuild src/run.ts --bundle --platform=node --outfile=lib/run.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/tool-cache": "^2.0.1",
    "semver": "^7.6.2"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "@types/semver": "^7.5.8",
    "esbuild": "^0.21.4",
    "typescript": "^5.4.5"
  }
}
